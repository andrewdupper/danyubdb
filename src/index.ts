// testing file

console.log("hello world");

// npm i danyubdb
// init?
// write danyubdb.config.ts
//     let user = new Entity(...);
//     let foo = new Entity(...);
//     let bar = new Entity(...);
//     let baz = new Entity(...);
//     let tree = new Tree(user).addChild(foo).addChild(bar);
//     let superTree = new Tree(baz).addChild(tree);
// database accesses are checked every build
// abstract codegen

// danyubdb.config.ts:
//     Entites
//     Tree
//     Access Patterns
 
// Entities:
// Label + Attributes

// Attribute
// Label + Type

// Tree
// Head TreeNode

// TreeNode
// Entity + List of TreeNode

// Access Patterns
//     Input: Attribute
//     Output: Entity

